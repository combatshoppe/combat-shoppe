node v16.14.0
npm v8.5.0

# Setup Dev Environment
run 'npm install'
run 'node server.js'
run 'bower install' on linux or 'npm install -g bower' on windows

# Links
https://www.tutorialspoint.com/meanjs/meanjs_project_setup.htm
https://www.vultr.com/docs/how-to-install-mongodb-4-0-on-arch-linux/

# Notes
- MongoDB seems to be server side, so we really only have to worry about MongoDB GET requests. We could even just do this once, have a client copy and save everything over to their browser (since it won't be many files), and never have them use the Mongo API again.
